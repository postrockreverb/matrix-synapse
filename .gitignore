# Secrets
.env

# Config files (copied from .example, contain secrets/paths)
docker-compose.yml
synapse/homeserver.yaml
coturn/turnserver.conf
element/config.json
livekit/config.yaml

# Synapse runtime files
synapse/*.signing.key
synapse/signing.key
synapse/*.pid

# Claude Code
.claude/
